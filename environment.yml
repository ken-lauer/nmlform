# To create an environment with this file, use the following:
# mamba env create -f environment.yml
name: nmlform
channels:
  - conda-forge
dependencies:
  - python=3.12
  # If you need MPI variants, use the following syntax for OpenMPI:
  # - hdf5 * mpi_openmpi*
  # or mpich:
  # - hdf5 * mpi_mpich*
  # Any custom pip packages can go here:
  # - pip:
  #   - git+git://github.com/bmad-sim/pytao
